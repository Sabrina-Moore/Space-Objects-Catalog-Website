Goal: Create a catalog of planetary and satellite (moons) objects that have cards that present information (like size, distance from Earth) 

Update 3 Changes made:
- created more data with a new array (large moons of each planet) and had to create separate showCards and editCardContent because the moon objects information is not the same as the planet objects
- Reworked some of the CSS styling - playing around with card colors and background images



Problems:
1. OG code used URL for images - should I switch to an images folder or stay with URL for larger datasets?
2. Attempting to import the dataset from csv/txt file did not work - how do I properly do that? 
3. Using an array of objects for a large dataset may be very slow, but I'm unfamiliar with maps


TO DO:
- Write search function
- Create filter (filtering by types)



Abandoned Ideas that are worth implementing later:
- I'd love it if each object card was a button that when clicked on would lead to a hidden page with more information (also the same thing for the search function)
- I also really want the dataset to come from a text file, but don't have the skills to figure out how to do it in js


